{
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
    // Usunięto "outputDirectory": "dist/public" z tego miejsca
  },
  "routes": [
    { "src": "/(.*)", "dest": "/dist/public/index.html" }
  ]
}
